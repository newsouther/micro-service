server:
  servlet:
    context-path: /microuser
  port: 2405


spring:
  application:
    name: micro-user
  cloud:
    nacos:
      discovery:
        server-addr: 5.189.166.137:8848
        group: micro-service
      config:
        server-addr: 5.189.166.137:8848 # nacos的服务端地址
        file-extension: yaml # 配置文件格式
        group: micro-service
        refresh-enabled: true